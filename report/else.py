
C++

if (balance>0)
	if((balance>amt)&&(balance<bal))
		print "balance is not null"
else
	print "balance is not"
###################################

python

if (balance>0)
	if((balance>amt)&&(balance<bal))
		print "balance is not null"
	else
		print "balance is not"
###################################

compare

if (balance>0)
	if((balance>amt)&&(balance<bal))
		print "balance is not null"
else
	print "balance is not"
###################################